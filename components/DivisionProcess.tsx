export function DivisionProcess({steps}:{steps:{number:string;title:string;text:string}[]}){return <div className={`division-process count-${steps.length}`}><div className="division-process-line" aria-hidden="true"/>{steps.map(step=><article key={step.number}><span className="division-process-node" aria-hidden="true"/><p>{step.number}</p><h3>{step.title}</h3><span>{step.text}</span></article>)}</div>}
