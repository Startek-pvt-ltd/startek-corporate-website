import { Reveal } from "./Reveal";
export function InnerHero({eyebrow,title,text,label}:{eyebrow:string;title:string;text:string;label?:string}){return <section className="inner-hero" id="home"><div className="inner-hero-grid" aria-hidden="true"/><div className="inner-hero-orbit" aria-hidden="true"><i/><i/><span/></div><div className="container inner-hero-content"><Reveal><p className="eyebrow"><span>+</span>{eyebrow}</p><h1>{title.split("\n").map((line,i)=><span key={line}>{line}{i<title.split("\n").length-1&&<br/>}</span>)}</h1><p>{text}</p>{label&&<div className="inner-hero-label">{label}</div>}</Reveal></div></section>}
