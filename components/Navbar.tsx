"use client";
import { useEffect,useState } from "react";
import Link from "next/link";
import { ArrowUpRight,ChevronDown,Menu,X } from "lucide-react";
import { divisions,navigation } from "@/data/site";
import { Logo } from "./Logo";
export function Navbar(){const [scrolled,setScrolled]=useState(false);const [open,setOpen]=useState(false);const [solutionsOpen,setSolutionsOpen]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>24);onScroll();addEventListener("scroll",onScroll,{passive:true});return()=>removeEventListener("scroll",onScroll)},[]);useEffect(()=>{document.body.style.overflow=open?"hidden":"";return()=>{document.body.style.overflow=""}},[open]);return <header className={`site-header ${scrolled?"scrolled":""}`}><div className="nav-shell"><Logo/><nav className="desktop-nav" aria-label="Main navigation">{navigation.map(item=>item.label==="Solutions"?<div className="nav-solutions" key={item.label} onMouseEnter={()=>setSolutionsOpen(true)} onMouseLeave={()=>setSolutionsOpen(false)}><button onClick={()=>setSolutionsOpen(v=>!v)} aria-expanded={solutionsOpen}>Solutions <ChevronDown size={13}/></button>{solutionsOpen&&<div className="mega-menu">{divisions.map((d,i)=><Link href={`#${d.id}`} key={d.id} onClick={()=>setSolutionsOpen(false)}><span>0{i+1}</span><div><strong>{d.name}</strong><small>{d.kicker}</small></div><ArrowUpRight size={16}/></Link>)}</div>}</div>:<Link key={item.label} href={item.href}>{item.label}</Link>)}</nav><Link className="button button-small nav-cta" href="#contact">Start a Project <ArrowUpRight size={14}/></Link><button className="menu-toggle" onClick={()=>setOpen(v=>!v)} aria-label={open?"Close menu":"Open menu"} aria-expanded={open}>{open?<X/>:<Menu/>}</button></div><div className={`mobile-menu ${open?"open":""}`} aria-hidden={!open}><nav aria-label="Mobile navigation">{navigation.map((item,i)=><Link key={item.label} href={item.href} onClick={()=>setOpen(false)}><span>0{i+1}</span>{item.label}</Link>)}</nav><div className="mobile-divisions">{divisions.map(d=><Link href={`#${d.id}`} key={d.id} onClick={()=>setOpen(false)}>{d.name}<ArrowUpRight size={14}/></Link>)}</div><Link className="button" href="#contact" onClick={()=>setOpen(false)}>Start a Project</Link></div></header>}
